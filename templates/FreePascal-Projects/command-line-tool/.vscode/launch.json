{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug FPC: %PROJECT_NAME%",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\%PROJECT_NAME%.exe",
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb",
            "preLaunchTask": "Build FPC: %PROJECT_NAME%",
            "stopAtEntry": false,
            "externalConsole": false
        }
    ]
}
