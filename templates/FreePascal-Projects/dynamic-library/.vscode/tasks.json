{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build FPC Library: %PROJECT_NAME%",
            "type": "fpc",
            "file": "${workspaceFolder}\\%PROJECT_NAME%.lpr",
            "cwd": "${workspaceFolder}",
            "buildOption": {
                "syntaxMode": "ObjFPC"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$fpc"
        }
    ]
}
